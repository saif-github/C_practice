#include<stdio.h>
 main() 
		{  int a;
			scanf("%d",&a);       //scan a negative integer; 
			if(a) 
				printf("%d\n",a); 
			else 
				printf("#\n"); 
		}
		/*here if a have some value it prints as it is
		or else for 0 it prints #    */
		/*it can scan for positive no */
