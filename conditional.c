#include<stdio.h>
int main()
{
	int a;
	scanf("%d",&a);
	if (a>=10)
	{
	a++;
	}
	else
	{
	--a;
	}
	printf("o/p =%d\n",a);
}
/*13. Scan a number to variable a, if a is more than 10, increment, else decrement.
		And print each time the value of a.*/
