#include<stdio.h>
void main() 
{
			int val=5,num;
			 printf("%d\n",scanf("%d %d",&val,&num)); //scanf only returns the no of elements
		}
		/* here we r initializing with val=5 then we r scaning so this value eraze while scanning*/
		/*now here while printing we get return value of scanf*/
